# Compliance dashboard

```bash
npm install
cp .env.example .env.local
npm run dev --workspace @suraksha-ar/dashboard
```

Without Supabase environment variables the dashboard displays labelled demo data. The public demo certificate is `CERT-DEAD2026ABCDEF01`.
